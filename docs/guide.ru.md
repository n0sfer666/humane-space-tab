# Как пользоваться Humane Space Tab

[English version](guide.md)

У приложения нет своего окна: оно живёт в меню-баре, а всё, что делает, происходит, пока
вы держите сочетание клавиш.

![Лента](images/ribbon.png)

## Первый запуск

1. Перенесите **Humane Space Tab.app** в `/Applications` и запустите.
2. Сборку, скачанную из Releases, macOS откажется открыть с первого раза — откройте
   **Системные настройки → Конфиденциальность и безопасность**, пролистайте вниз и нажмите
   **Всё равно открыть**, затем запустите ещё раз.
3. При старте приложение просит **Универсальный доступ** (Accessibility). Выдайте его в
   **Системные настройки → Конфиденциальность и безопасность → Универсальный доступ**.
   Переключатель начинает работать через пару секунд, перезапуск не нужен.

Иконка в меню-баре — единственное подтверждение, что приложение работает: `⇄`, когда всё
в порядке, и треугольник с восклицательным знаком, когда нет. macOS привязывает выданный
доступ к подписи сборки, поэтому после каждого обновления его нужно выдать заново.

## Переключение между приложениями

Держите **⌘** и нажимайте **Tab**. Что произойдёт, зависит от того, как долго держите:

- **короткое нажатие** — быстрее задержки показа (по умолчанию 120 мс) — переключает на
  предыдущее приложение, ничего не показывая, как того ждёт мышечная память;
- **не отпускайте ⌘** — появится лента с приложениями **текущего Space**, недавно
  использованные первыми. Каждое следующее **Tab** двигает выделение на шаг.

Пока лента открыта:

| Клавиша | Что делает |
|---|---|
| **Tab** | шаг вперёд |
| **⇧Tab** | шаг назад |
| **Escape** | отмена — ничего не переключается |
| отпустить **⌘** | переключиться на выделенное приложение |

Выделенное приложение — иконка крупнее и ярче остальных, с именем под ней; соседи
приглушены. Начиная с пяти приложений лента становится каруселью: выделение стоит на
месте, а иконки едут под ним, поэтому шаг всегда одно и то же расстояние для глаза, и у
списка нет концов — за последним приложением приходит первое. Меньше пяти — каждая иконка
стоит на своём месте, движется только выделение. Панель перестаёт расти на десяти слотах.

## Переключение между окнами одного приложения

Держите **⌘** и нажимайте **`** (клавиша над Tab). Лента покажет окна приложения, которое
сейчас впереди, — только те, что на текущем Space, в порядке стопки, каждое со своим
заголовком. Жест тот же: `⇧` разворачивает направление, Escape отменяет, отпущенный **⌘**
поднимает окно.

Системное сочетание считает окна всех Space сразу — поэтому оно и утаскивает на соседний
рабочий стол. Это — не может: окна, которого здесь нет, нет и в списке, так что Space не
меняется и свёрнутое не разворачивается.

## Мышь

Пока лента открыта, указатель работает по ней:

- **двиньте мышь** над иконкой, чтобы выделить её — указатель, который просто стоял там,
  где открылась лента, ничего не меняет, пока не сдвинется;
- **клик** по иконке переключает сразу, не дожидаясь, пока отпустят **⌘**;
- **прокрутка** над лентой листает выделение.

## Настройки

Открываются из меню иконки в меню-баре → **Settings…**.

![Настройки](images/settings.png)

| Настройка | Что это |
|---|---|
| **Applications** | сочетание, открывающее ленту. Кликните в поле и наберите своё, либо **Restore default** вернёт `⌘Tab`. |
| **Windows of the front app** | второе сочетание, по умолчанию `` ⌘` ``. |
| **Show the ribbon on** | экран с фокусом клавиатуры или экран, где указатель. |
| **Reveal delay** | сколько держать сочетание до появления ленты — от 0 до 500 мс. На `0` лента появляется сразу. |
| **Open at login** | регистрирует приложение в macOS как элемент входа. |
| **Switch between windows, not applications** | `⌘Tab` начинает перечислять окна, как в Windows и Linux. По умолчанию выключено: два документа одного редактора станут двумя записями. |
| **Use the private Space layer** | приватный слой знает, какому Space принадлежит свёрнутое окно, но он недокументирован. Выключено — приложение смотрит только на окна на экране. |

Сочетанию нужен модификатор, который держат, и рекордер отказывает в том, что дизайн не
может выполнить: без модификатора, с уже входящим **⇧** (это обратное направление), с
**Escape**, `⌘Q` и `⌘W` — и говорит, что именно не так. Сочетание рисуется клавишами
текущей раскладки, поэтому на русской видно `⌘Ё` там, где на английской `` ⌘` ``.

## Меню-бар

| Пункт | Когда он есть |
|---|---|
| **Grant Accessibility…** | пока доступа нет — просит ещё раз |
| **Open Input Monitoring…** | когда macOS удерживает нажатия клавиш (см. ниже) |
| **Settings…** | всегда |
| **Copy Inventory** | всегда — копирует список приложений текущего Space в буфер обмена; это то, что стоит приложить к баг-репорту |
| **Quit Humane Space Tab** | всегда |

## Если что-то не так

**Нет иконки в меню-баре.** Если слева от чёлки не осталось свободного места, macOS
выбрасывает иконку: система не рисует статус-айтем, для которого нет слота, и приложение
на это повлиять не может. Освободите место или откройте приложение снова из Finder или
Spotlight — второй запуск открывает настройки, а не вторую копию.

**Иконка говорит, что macOS удерживает нажатия.** Старая запись приложения запрещает ему
это в **Системные настройки → Конфиденциальность и безопасность → Мониторинг ввода**.
Удалите запись кнопкой **−** — разрешение приложению не нужно, нужно отсутствие запрета —
и кликните обратно в приложение: тап пересоздаётся без перезапуска.

**`⌘Tab` по-прежнему системный.** Доступа нет либо он выдан предыдущей сборке. Нажмите
**Grant Accessibility…**, а если запись в списке уже есть — удалите её кнопкой **−** и
выдайте доступ заново.

**«macOS cannot register this copy» под «Open at login».** `SMAppService` отказывается
регистрировать бандл; на копии, собранной локально и запускавшейся из нескольких
сборочных каталогов, это воспроизводится. Известно и не исправлено — на остальное не
влияет.

## Что приложение видит

У него есть Универсальный доступ — разрешение широкое, и сделано всё, чтобы пользоваться
им по минимуму: сетевого кода нет вообще, AppleEvents нет, плагинов нет; нажатия
проверяются только на совпадение с вашим сочетанием; заголовки окон читаются, только пока
включено переключение по окнам, и только чтобы нарисовать их; указатель виден лишь над
панелью самой ленты. Screen Recording не запрашивается никогда — поэтому превью окон здесь
и нет. Полная модель угроз — [S00](specs/S00-threat-model.md).
